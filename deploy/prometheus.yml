global:
  scrape_interval: 15s
rule_files: [alerting.yaml]
scrape_configs:
  - job_name: claims-agent
    static_configs:
      - targets: [app:8001]
    # 开启API认证时，通过组织内secret挂载配置http_headers，勿写入仓库。
